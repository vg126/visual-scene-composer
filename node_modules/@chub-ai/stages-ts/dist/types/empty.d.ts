import { StageBase, StageResponse } from "./stage";
import { Message } from "./message";
import { GenerationService } from "./generation/service";
import { LoadResponse } from "./load";
import { MessagingService } from "./messaging/service";
import React from "react";
/***
 @description An empty stage, that may be useful to extend when making tools.
 ***/
export declare class EmptyStage implements StageBase<any, any, any, any> {
    afterResponse(botMessage: Message): Promise<Partial<StageResponse<any, any>>>;
    beforePrompt(inputMessage: Message): Promise<Partial<StageResponse<any, any>>>;
    generator: GenerationService;
    load(): Promise<Partial<LoadResponse<any, any, any>>>;
    messenger: MessagingService<any>;
    render(): React.ReactElement;
    setState(state: any): Promise<void>;
}
